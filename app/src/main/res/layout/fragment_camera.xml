<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.camera.CameraFragment"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:background="#FF141414">

    <androidx.camera.view.PreviewView
        android:keepScreenOn="true"
        android:layout_margin="10dp"
        android:id="@+id/camera_viewfinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/camera_button"/>
    <!-- android:layout_above="@id/picture_title"/>

    REPLACE WITH RELATIVE OR LINEAR LAYOUT TO ADD EDIT TEXT ///// OR GO TO NEW FRAGMENT ONCE PICTURE TAKEN?
    <TextView
        android:id="@+id/picture_title"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="5dp"
        android:text="@string/picture_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/camera_button"/>
    -->

    <Button
        android:paddingTop="11dp"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/flash_button"
        android:layout_marginStart="66dp"
        android:layout_marginBottom="30dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:drawableTop="@drawable/ic_flash_off_36dp"
        android:background="@android:color/transparent"/>

    <Button
        android:radius="64dp"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/camera_button"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="30dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:background="@drawable/camera_button"
        android:drawableTop="@drawable/ic_camera_36dp"/>

    <Button
        android:paddingBottom="7dp"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/flip_button"
        android:layout_marginEnd="66dp"
        android:layout_marginBottom="30dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:drawableTop="@drawable/ic_flip_48dp"
        android:background="@android:color/transparent"/>

</RelativeLayout>