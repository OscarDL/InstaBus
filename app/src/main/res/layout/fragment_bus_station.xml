<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="#FF141414"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/bus_station_fragment"
    tools:context=".ui.bus_station.BusStationFragment">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            tools:listitem="@layout/bus_station_list_item"
            android:id="@+id/bus_station_recycler_view"
            android:layout_marginHorizontal="10dp"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:paddingVertical="5dp"
            android:clipToPadding="false"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            app:tint="@color/white"
            android:tint="@color/white"
            android:layout_margin="20dp"
            android:id="@+id/add_picture"
            android:scaleType="centerInside"
            android:layout_gravity="bottom|end"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/icon"
            app:backgroundTint="@color/accent_color"
            app:srcCompat="@drawable/ic_add_picture_24dp"/>

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>